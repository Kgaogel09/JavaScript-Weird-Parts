const students = ['Jack', 'John', 'Jill'];
const total = 3;

const add = (x, y) => {
  return x + y;
};

const multiply = (x, y) => {
  return x * y;
};

export { students, add, multiply };

export default total;
